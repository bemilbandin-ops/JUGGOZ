# Workspace Rules

## Electric Comets and Volumetric Fan Rays Modification Lock
You must NEVER modify, edit, delete, or refactor any code relating to the **Electric Comets** preset (including `emitComets`, `renderComets`, and the `electric-comets` logic inside `connectActiveClubs`) or the **Volumetric Fan Rays** preset (including `renderVolumetricFanRays` and its settings) inside `src/pixelPoi.ts`, unless the user explicitly and specifically instructs you to do so in a new prompt. Even if the user says "all" (e.g. "change all presets"), these locked settings and code files are NOT included and must remain completely untouched.
